module

public import LeanerVM.Parameters.Basic

/-!
# VM semantics

This layer will hold the source-faithful instruction set, machine state, memory model,
bytecode semantics, and executable reference interpreter. It must not depend on the
arithmetization or proof-system layers.
-/

namespace LeanerVM.Semantics

public section

end
end LeanerVM.Semantics
