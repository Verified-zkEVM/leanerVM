module

public import LeanerVM.Arithmetization.Basic

/-!
# Protocol assembly

This layer will assemble upstream proof-system components around the verified
arithmetization. Reusable polynomial, oracle, and proof-system theory belongs upstream;
only leanVM-specific composition stays here.
-/

namespace LeanerVM.Protocol

public section

end
end LeanerVM.Protocol
